# Frontend Environment Variables
# Copy this file to .env.local for local development.
# Set these in Vercel project settings for production.

# Backend API URL — required for production (Vercel).
# Local dev does NOT need this; Vite proxy handles /api → localhost:8080.
# Example: https://your-app.railway.app/api
VITE_API_URL=
